$('.articles').prepend("<%= escape_javascript(render partial: 'articles/article', locals: { article: @article }) %>");

$(".recent_body").html("<%= escape_javascript(render('articles/recent_entry'))%>");


$('.js-modal').fadeOut();
$('.js-form')[0].reset();
$('.js-message-errors').empty(); // 追加